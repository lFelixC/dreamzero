#!/bin/bash
set -euo pipefail

# Compatibility entry. The canonical DROID Wan2.2 MoT launcher is:
#   scripts/train/droid_wan22_mot_fseq200.sh

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
export DREAMZERO_ROOT="${DREAMZERO_ROOT:-/data/dreamzero_mot}"
export PYTHON_BIN="${PYTHON_BIN:-/data/dreamzero/.venv/bin/python}"
export CHECKPOINT_ROOT="${CHECKPOINT_ROOT:-/data/checkpoints/dreamzero}"
export DATASET_ROOT="${DATASET_ROOT:-/data/datasets/dreamzero/droid_lerobot}"
export WAN22_CKPT_DIR="${WAN22_CKPT_DIR:-${CHECKPOINT_ROOT}/Wan2.2-TI2V-5B}"
export IMAGE_ENCODER_DIR="${IMAGE_ENCODER_DIR:-${WAN22_CKPT_DIR}}"
export TOKENIZER_DIR="${TOKENIZER_DIR:-${WAN22_CKPT_DIR}/google/umt5-xxl}"
export OUTPUT_DIR="${OUTPUT_DIR:-${CHECKPOINT_ROOT}/dreamzero_droid_wan22_mot_mix_att_anchor_full}"
export TRAIN_ARCHITECTURE="${TRAIN_ARCHITECTURE:-full}"
export CUDA_VISIBLE_DEVICES="${CUDA_VISIBLE_DEVICES:-4,5,6,7}"
export PER_DEVICE_BS="${PER_DEVICE_BS:-96}"
export MAX_STEPS="${MAX_STEPS:-30000}"
export EVAL_STEPS="${EVAL_STEPS:-100}"
export SAVE_STEPS="${SAVE_STEPS:-10000}"
export SAVE_TOTAL_LIMIT="${SAVE_TOTAL_LIMIT:-5}"
export DEEPSPEED_CFG="${DEEPSPEED_CFG:-zero2}"
export LEARNING_RATE="${LEARNING_RATE:-2e-5}"
export MAX_CHUNK_SIZE="${MAX_CHUNK_SIZE:-4}"
export DATALOADER_NUM_WORKERS="${DATALOADER_NUM_WORKERS:-8}"
export DATALOADER_PREFETCH_FACTOR="${DATALOADER_PREFETCH_FACTOR:-4}"
export DATALOADER_PERSISTENT_WORKERS="${DATALOADER_PERSISTENT_WORKERS:-true}"
export MOT_ACTION_VIDEO_ATTENTION="${MOT_ACTION_VIDEO_ATTENTION:-first_frame}"
export MOT_ACTION_VIDEO_KI="${MOT_ACTION_VIDEO_KI:-${MOT_KI:-false}}"
export DROID_RANDOM_DROP_EXTERIOR_VIEW_PROB="${DROID_RANDOM_DROP_EXTERIOR_VIEW_PROB:-0.0}"
export DATASET_SHARD_SAMPLING_RATE="${DATASET_SHARD_SAMPLING_RATE:-0.1}"
export TARGET_VIDEO_HEIGHT="${TARGET_VIDEO_HEIGHT:-${MODEL_TARGET_HEIGHT:-160}}"
export TARGET_VIDEO_WIDTH="${TARGET_VIDEO_WIDTH:-${MODEL_TARGET_WIDTH:-320}}"
export FRAME_SEQLEN="${FRAME_SEQLEN:-${MODEL_FRAME_SEQLEN:-50}}"
export SWANLAB_SYNC_WANDB="${SWANLAB_SYNC_WANDB:-1}"
exec "${SCRIPT_DIR}/droid_wan22_mot_fseq200.sh" "$@"
